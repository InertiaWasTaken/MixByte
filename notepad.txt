ttyd installed/set on boot first
venv installed/set on boot second
usbm installed/set on boot third

next we need to make it so usb mode is started before anything, then run ttyd, then run the webpage
